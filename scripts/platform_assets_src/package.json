{
  "name": "app-factory-platform-assets",
  "private": true,
  "description": "Build inputs for Basecoat/Tailwind chrome. HTMX and Alpine are fetched without npm lock pins. Maintainer-only.",
  "scripts": {
    "build:css": "tailwindcss -i ./src/input.css -o ./dist/basecoat-factory.min.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "4.3.3",
    "basecoat-css": "1.0.2",
    "tailwindcss": "4.3.3"
  }
}
