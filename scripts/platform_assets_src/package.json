{
  "name": "app-factory-platform-assets",
  "private": true,
  "description": "Build inputs for Basecoat/Alpine/Tailwind chrome. HTMX is fetched from GitHub, not npm. Maintainer-only.",
  "scripts": {
    "build:css": "tailwindcss -i ./src/input.css -o ./dist/basecoat-factory.min.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "4.3.3",
    "alpinejs": "3.17.2",
    "basecoat-css": "1.0.2",
    "tailwindcss": "4.3.3"
  }
}
